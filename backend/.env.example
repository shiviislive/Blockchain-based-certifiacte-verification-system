# Copy to .env and set these values
# RPC endpoint (e.g. local hardhat or Infura/Alchemy)
RPC_URL=http://127.0.0.1:8545
# Deployer private key (for issuing certificates)
PRIVATE_KEY=
# Deployed contract address
CONTRACT_ADDRESS=
